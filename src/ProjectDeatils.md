<!-- 
1.Clone the repository using version control.
2.Clone URL: git@github.com:vivebharath/Rigi-Assignment.git
Run npm start.
Project Structure:

The starting point of the application is the app.js file, where data is obtained from the Date function.

Components Used:
VideoPlayer: 
This component is responsible for video playback using the ReactPlayer component, which is imported from 'react-player' (https://www.npmjs.com/package/react-player).

PlayList:
Displays the playlist videos and facilitates playlist reordering and searching. This component also includes the NowPlayingComponent.

NowPlayingComponent: 
Simply shows which video is currently playing.
 -->